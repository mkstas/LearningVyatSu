\documentclass[a4paper,14pt]{extarticle}

\usepackage[a4paper,top=20mm,bottom=20mm,left=30mm,right=10mm]{geometry}
\usepackage[T1,T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usepackage{indentfirst}
\usepackage{titlesec}
\usepackage{graphicx}
\usepackage{verbatim}
\usepackage{fancyvrb}

\renewcommand{\baselinestretch}{1.3}
\titleformat{\section}{\normalsize\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesection}{1em}{}
\setlength{\parindent}{12.5mm}

\begin{document}

  \newpage\thispagestyle{empty}
  \begin{center}
    \MakeUppercase{
      Министерство науки и высшего образования Российской Федерации\\
      Федеральное государственное бюджетное образовательное учреждение высшего образования\\
      <<Вятский Государственный Университет>>\\
    }
    Институт математики и информационных систем\\
    Факультет автоматики и вычислительной техники\\
    Кафедра электронных вычислительных машин
  \end{center}
  \vfill

  \begin{center}
    Отчет по лабораторной работе №2\\
    по дисциплине\\
    <<Технологии программирования>>\\
  \end{center}
  \vfill

  \noindent
  \begin{tabular}{ll}
    Выполнил студент гр. ИВТб-2301-05-00 \hspace{5mm} &
    \rule[-1mm]{25mm}{0.10mm}\,/Макаров С.А./\\
    
    Проверил преподаватель & \rule[-1mm]{25mm}{0.10mm}\,/Пащенко Д.Э./\\
  \end{tabular}

  \vfill
  \begin{center}
    Киров 2025
  \end{center}

  \newpage
  \section*{Цель}
  Цель: Разработать веб-приложение с клиент-серверной архитектурой, реализующее взаимодействие между клиентско и серверной частью через отправку и обработку данных формы.

  \section*{Задание}
  Сверстать сайт с формой отправки данных, форма должна содержать поля ввода и кнопку отправку формы. Html, css и js должны находится в разных файлах. Данные из формы отправляются на локальный сервер(node js, express js), сервер возвращает ответ. Для сервера необходимо использовать node js с фреймворком Express.

  \section*{Решение}

  \begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{img/1.png}
  \end{figure}
  \begin{center}
    Рисунок 1 – Страница веб-приложения
  \end{center}

  \pagebreak
  \begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{img/2.png}
  \end{figure}
  \begin{center}
    Рисунок 2 – Корзина с товарами
  \end{center}

  \begin{figure}[h]
    \centering
    \includegraphics[width=1\linewidth]{img/3.png}
  \end{figure}
  \begin{center}
    Рисунок 3 – Форма оформения заказа
  \end{center}

  Исходный код серверной стороны представлен ниже:

  \noindent
  \begin{Verbatim}[tabsize=4,fontsize=\small]
import express, { type Request, type Response } from 'express';
import path from 'path';
import { PrismaClient, type Product } from './generated/prisma/index.js';

const prisma = new PrismaClient();
const app = express();

app.use(express.static(path.resolve(process.cwd(), 'public')));
app.use(express.json());

app.get('/', (req: Request, res: Response) => {
  res.sendFile(path.resolve(process.cwd(), 'index.html'));
});

app.get('/api/v1/products', async (req: Request, res: Response) => {
  const products = await prisma.product.findMany();

  res.status(200).send(products);
});

app.post('/api/v1/orders', async (req: Request, res: Response) => {
  const order = await prisma.order.create({
    data: {
      username: req.body.username,
      phoneNumber: req.body.phone_number,
      cardNumber: req.body.card_number,
    },
  });

  req.body.cart.forEach(async (product: Product) => {
    await prisma.orderProduct.create({
      data: {
        orderId: order.id,
        productId: product.id,
      },
    });
  });

  res.status(200).send();
});

app.listen(3000, () => {
  console.log('Listening on http://localhost:3000');
});
  \end{Verbatim}

  Исходный код html файла веб-приложения:

  \noindent
  \begin{Verbatim}[tabsize=4,fontsize=\small]
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Пирожочки</title>
    <link rel="stylesheet" href="./styles/main.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Rubik:ital,
        wght@0,300..900;1,300..900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="app">
      <header class="header">
        <div class="header__logo">
          <h1 class="header__logo-caption">Пирожочки</h1>
        </div>
        <button class="header__cart-button button" type="button">
          <span class="header__cart-sum">0 руб.</span>
          <img src="img/shopping-cart.svg" alt="Корзина" />
        </button>
      </header>

      <section class="products"></section>
    </div>

    <aside class="drawer">
      <div class="drawer__head">
        <button class="drawer__back-button" type="button">
          <img src="img/chevron-left.svg" alt="" />
        </button>
        <h2 class="drawer__title">Корзина</h2>
      </div>

      <div class="drawer__list"></div>

      <div class="drawer__footer">
        <div class="drawer__total">
          <div class="drawer__total-label">Стоимость</div>
          <div class="drawer__total-dash"></div>
          <div class="drawer__total-value">0 руб.</div>
        </div>
        <button class="drawer__checkout-button button" type="button">
          Перейти к оформлению
        </button>
      </div>
    </aside>

    <div class="payment">
      <form class="payment__form" method="POST">
        <button class="payment__close-button" type="button">
          <img src="img/x-white.svg" alt="Закрыть" />
        </button>

        <div class="payment__row">
          <div class="payment__field">
            <label for="username">Имя</label>
            <input
              id="username"
              placeholder="Аркадий"
              name="username"
              type="text"
              class="payment__input"
            />
          </div>
          <div class="payment__field">
            <label for="phone_number">Номер телефона</label>
            <input
              id="phone_number"
              placeholder="+7 (900) 000 00 00"
              name="phone_number"
              type="text"
              class="payment__input"
            />
          </div>
        </div>

        <div class="payment__row">
          <div class="payment__field">
            <label for="card_number">Номер карты</label>
            <input
              id="card_number"
              placeholder="0000 0000 0000 0000"
              name="card_number"
              type="text"
              class="payment__input"
            />
          </div>
          <div class="payment__field">
            <label for="cvv_code">CVV код</label>
            <input
              id="cvv_code"
              placeholder="000"
              name="cvv_code"
              type="text"
              class="payment__input"
            />
          </div>
        </div>

        <button class="payment__submit-button button" type="submit">
          Оформить заказ
        </button>
      </form>
    </div>

    <script src="https://unpkg.com/imask"></script>
    <script src="./scripts/main.js"></script>
  </body>
</html>
  \end{Verbatim}

  Исходный код каскадных таблиц стилей веб-приложения:

  \noindent
  \begin{Verbatim}[tabsize=4,fontsize=\small]
:root {
  --color-white: #ffffff;
  --color-black: #0f172a;
  --color-grey: #e2e8f0;
  --color-brown: #854d0e;
  --color-brown-light: #a16207;
  --radius-sm: 12px;
  --radius-md: 16px;
  --radius-lg: 24px;
  --transition: 0.2s ease;
}

*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Rubik', sans-serif;
  background: var(--color-white);
  color: var(--color-black);
}

button,
input {
  font-family: inherit;
}

.body--lock {
  overflow: hidden;
}

.button {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  min-width: 120px;
  padding: 12px 16px;
  border: 1px solid var(--color-brown);
  border-radius: var(--radius-md);
  background: var(--color-brown);
  font-weight: 500;
  color: var(--color-white);
  cursor: pointer;
  transition: all var(--transition);
}

.button--outline {
  background: var(--color-white);
  color: var(--color-brown);
}

.button:hover {
  background: var(--color-brown-light);
  border-color: var(--color-brown-light);
  color: var(--color-white);
}

.button:focus-visible {
  outline: 2px solid var(--color-brown-light);
  outline-offset: 2px;
}

.button:disabled {
  background: var(--color-brown);
  border-color: var(--color-brown);
  cursor: not-allowed;
}

.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  overflow: hidden;
  clip: rect(0 0 0 0);
}

.app {
  max-width: 1140px;
  padding: 0 16px;
  margin: 0 auto;
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16px;
  padding: 16px 0;
  border-bottom: 1px solid var(--color-grey);
}

.header__logo {
  display: flex;
  align-items: center;
  gap: 8px;
}

.header__logo-caption {
  font-size: 24px;
  font-weight: 700;
  letter-spacing: 4px;
  text-transform: uppercase;
}

.header__cart-button img {
  width: 16px;
  height: 16px;
}

.products {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 32px 16px;
  padding: 64px 0;
}

.card {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.card__image {
  width: 100%;
  border-radius: var(--radius-md);
  object-fit: cover;
}

.card__title {
  flex: 1;
  font-size: 18px;
  font-weight: 400;
}

.card__row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.card__price {
  font-size: 24px;
  font-weight: 500;
}

.drawer {
  position: fixed;
  inset: 0 0 0 auto;
  width: 100%;
  max-width: 480px;
  display: flex;
  flex-direction: column;
  gap: 32px;
  padding: 16px;
  background: var(--color-white);
  border-left: 1px solid var(--color-grey);
  transform: translateX(100%);
  transition: transform var(--transition);
  pointer-events: none;
}

.drawer--active {
  transform: translateX(0);
  pointer-events: all;
}

.drawer__head {
  display: flex;
  align-items: center;
  gap: 16px;
}

.drawer__back-button {
  display: flex;
  padding: 8px;
  background: none;
  border: none;
  cursor: pointer;
}

.drawer__back-button img {
  transition: transform var(--transition);
}

.drawer__back-button:hover img {
  transform: translateX(-4px);
}

.drawer__title {
  font-size: 20px;
  font-weight: 500;
}

.drawer__list {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 16px;
  overflow-y: auto;
}

.drawer__item {
  display: flex;
  gap: 16px;
  align-items: flex-start;
}

.drawer__item-image {
  width: 100px;
  border-radius: var(--radius-sm);
  flex-shrink: 0;
}

.drawer__item-info {
  flex: 1;
  display: grid;
  gap: 8px;
}

.drawer__item-title {
  font-size: 16px;
  font-weight: 400;
}

.drawer__item-price {
  font-size: 20px;
  font-weight: 500;
}

.drawer__item-remove {
  display: flex;
  padding: 8px;
  background: none;
  border: none;
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: background var(--transition);
}

.drawer__item-remove:hover {
  background: var(--color-grey);
}

.drawer__item-remove img {
  width: 20px;
  height: 20px;
}

.drawer__footer {
  display: grid;
  gap: 16px;
}

.drawer__total {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 20px;
}

.drawer__total-value {
  font-weight: 500;
}

.drawer__total-dash {
  flex: 1;
  margin-bottom: -12px;
  border-bottom: 1px dashed var(--color-black);
}

.payment {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.4);
  align-items: center;
  justify-content: center;
  padding: 16px;
}

.payment--active {
  display: flex;
}

.payment__form {
  position: relative;
  width: 100%;
  max-width: 640px;
  padding: 24px;
  background: var(--color-white);
  border-radius: var(--radius-lg);
  display: grid;
  gap: 24px;
}

.payment__close-button {
  position: absolute;
  top: -16px;
  right: -48px;
  padding: 8px;
  background: none;
  border: none;
  cursor: pointer;
}

.payment__close-button img {
  width: 32px;
  height: 32px;
}

.payment__row {
  display: flex;
  gap: 24px;
}

.payment__field {
  flex: 1;
  display: grid;
  gap: 4px;
}

.payment__field label {
  font-size: 14px;
  font-weight: 500;
}

.payment__input {
  padding: 12px 16px;
  border: 1px solid var(--color-grey);
  border-radius: var(--radius-md);
  transition: border var(--transition);
}

.payment__input:focus {
  outline: none;
  border-color: var(--color-brown);
}

@media (max-width: 1023px) {
  .products {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (max-width: 767px) {
  .products {
    grid-template-columns: 1fr 1fr;
  }

  .payment__close-button {
    top: 16px;
    right: 16px;
    background: rgba(255, 255, 255, 0.9);
    border-radius: 50%;
  }
}

@media (max-width: 479px) {
  .products {
    grid-template-columns: 1fr;
  }

  .payment__row {
    flex-direction: column;
  }
}
  \end{Verbatim}

  Исходный код JavaSсript веб-приложения:

  \noindent
  \begin{Verbatim}[tabsize=4,fontsize=\small]
const productsElement = document.querySelector('.products');
const drawerElement = document.querySelector('.drawer');
const drawerBackButton = document.querySelector('.drawer__back-button');
const drawerListElement = document.querySelector('.drawer__list');
const drawerTotalValueElement = 
  document.querySelector('.drawer__total-value');
const drawerCheckoutButton = 
  document.querySelector('.drawer__checkout-button');
const headerCartButton = 
  document.querySelector('.header__cart-button');
const paymentElement = document.querySelector('.payment');
const paymentForm = document.querySelector('.payment__form');
const closePaymentFormButton = 
  document.querySelector('.payment__close-button');
const phoneNumberInput = document.querySelector('#phone_number');
const cardNumberInput = document.querySelector('#card_number');
const cvvCodeInput = document.querySelector('#cvv_code');

IMask(phoneNumberInput, { mask: '+{7} (000) 000 00 00' });
IMask(cardNumberInput, { mask: '0000 0000 0000 0000' });
IMask(cvvCodeInput, { mask: '000' });

headerCartButton.addEventListener('click', 
  () => drawerElement.classList.toggle('drawer--active'));
drawerBackButton.addEventListener('click', 
  () => drawerElement.classList.remove('drawer--active'));

drawerCheckoutButton.addEventListener('click', () => {
  paymentElement.classList.add('payment--active');
  document.body.classList.add('body--lock');
});

closePaymentFormButton.addEventListener('click', () => {
  paymentElement.classList.remove('payment--active');
  document.body.classList.remove('body--lock');
});

const getCart = () => {
  return JSON.parse(localStorage.getItem('cart')) ?? [];
};

const setCart = cart => {
  localStorage.setItem('cart', JSON.stringify(cart));
};

const getTotalCost = cart => {
  return cart.reduce((acc, item) => acc + item.price, 0);
};

const updateHeaderCartCost = () => {
  headerCartButton.querySelector('span').innerText = 
    getTotalCost(getCart()) + ' руб.';
};

const updateTotalDrawerCost = () => {
  drawerTotalValueElement.innerText = getTotalCost(getCart()) + ' руб.';
};

updateHeaderCartCost();
updateTotalDrawerCost();

const checkAndDisableCheckoutButton = () => {
  if (getTotalCost(getCart()) === 0) {
    drawerCheckoutButton.disabled = true;
  } else {
    drawerCheckoutButton.disabled = false;
  }
};

checkAndDisableCheckoutButton();

const setCardButtonToAdd = button => {
  button.innerText = 'В корзину';
  button.classList.remove('button--outline');
};

const setCardButtonToDelete = button => {
  button.innerText = 'Удалить';
  button.classList.add('button--outline');
};

const updateCart = product => {
  const cart = getCart();
  const isInCart = !!cart.find(item => item.id === product.id);

  if (isInCart) {
    setCart(cart.filter(item => item.id !== product.id));
    setCardButtonToAdd(productsElement.querySelector(
      `[data-product-id="${product.id}"]`
    ));
  }

  if (!isInCart) {
    cart.push(product);
    setCart(cart);
    setCardButtonToDelete(productsElement.querySelector(
      `[data-product-id="${product.id}"]`
    ));
  }

  updateHeaderCartCost();
  updateTotalDrawerCost();
  checkAndDisableCheckoutButton();
};

const getProducts = async () => {
  try {
    const response = await fetch('/api/v1/products');
    const products = await response.json();

    return products;
  } catch (error) {
    console.error(error);
  }
};

const createCartProductElement = product => {
  return `
    <div class="drawer__item">
      <img class="drawer__item-image" 
        src="${product.imageUrl}" alt="${product.title}" />
      <div class="drawer__item-info">
        <h3 class="drawer__item-title">${product.title}</h3>
        <p class="drawer__item-price">${product.price} руб.</p>
      </div>
      <button data-product-id=${product.id} class="drawer__item-remove">
        <img src="img/x-black.svg" alt="" />
      </button>
    </div>`;
};

const createProductElement = (product, cart) => {
  const isInCart = !!cart.find(item => item.id === product.id);

  return `
    <article class="card">
      <img src="${product.imageUrl}" alt="${product.title}" 
        class="card__image" />
      <h3 class="card__title">${product.title}</h3>
      <div class="card__row">
        <span class="card__price">${product.price} руб.</span>
        <button data-product-id=${product.id} 
          class="button${isInCart ? ' button--outline' : ''}">
          ${isInCart ? 'Убрать' : 'В корзину'}
        </button>
      </div>
    <article>`;
};

const displayCartProducts = () => {
  cart = getCart();
  drawerListElement.innerHTML = '';

  if (cart.length === 0) return;

  cart.forEach(product => {
    drawerListElement.innerHTML += createCartProductElement(product);
  });

  cart.forEach(product => {
    drawerListElement
      .querySelector(`[data-product-id="${product.id}"]`)
      .addEventListener('click', () => {
        updateCart(product);
        displayCartProducts();
      });
  });
};

displayCartProducts();

const displayProducts = async () => {
  const products = await getProducts();
  const cart = getCart();

  products.forEach(product => {
    productsElement.innerHTML += createProductElement(product, cart);
  });

  products.forEach(product => {
    productsElement
      .querySelector(`[data-product-id="${product.id}"]`)
      .addEventListener('click', () => {
        updateCart(product);
        displayCartProducts();
      });
  });
};

displayProducts();

paymentForm.addEventListener('submit', async e => {
  e.preventDefault();

  const formData = new FormData(e.target);
  const jsonData = Object.fromEntries(formData);
  jsonData.cart = getCart();

  try {
    const response = await fetch('/api/v1/orders', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(jsonData),
    });

    if (response.ok) {
      setCart([]);
      displayCartProducts();
      updateHeaderCartCost();
      updateTotalDrawerCost();

      paymentForm.reset();
      paymentElement.classList.remove('payment--active');
      drawerElement.classList.remove('drawer--active');
      productsElement.innerHTML = '';

      displayProducts();
    }
  } catch (error) {
    console.error(error);
  }
});
  \end{Verbatim}

  \section*{Вывод}
  В ходе выполнения лабораторной работы разработано веб-приложение, работающее по клиент-серверной архитектуре. Поставленные задачи выполнены в полном объеме.

\end{document}